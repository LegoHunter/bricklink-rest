# bricklink-model

DTO model classes for the documented BrickLink REST API resources.

This module intentionally contains only API resource model types and supporting JSON serialization utilities. REST client implementation code belongs in `bricklink-rest-client`.
